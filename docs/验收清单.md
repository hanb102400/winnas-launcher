# 验收清单

> 对照设计文档第 2 章 FR/NFR，如实标记实现状态。
> ✅ 已完成　⚠️ 部分完成　❌ 未实现

## 功能需求（FR）

| 编号 | 需求 | 状态 | 说明 |
|------|------|------|------|
| FR-01 | 开机直达 Launcher | ✅ | 自启（注册表 Run）+ 全屏置顶，不替换 Explorer |
| FR-02 | 无边框独占全屏 | ✅ | `fullscreen` + `decorations:false` + 原生置顶 |
| FR-03 | 抢焦点防护 | ⚠️ | 焦点状态机（Idle/AppRunning）+ 置顶跟随焦点已做；防系统弹窗主动夺回未完整 |
| FR-04 | 焦点导航系统 | ✅ | 二维网格 + 分页翻页 + 齿轮/设置/弹窗焦点隔离 |
| FR-05 | 遥控器按键捕获 | ⚠️ | 方向/确认/返回/菜单/音量键已捕获；**可重映射未做** |
| FR-06 | 拦截系统快捷键 | ✅ | Win/Win+D/Win+Tab/Alt+F4/Ctrl+Shift+Esc |
| FR-07 | 隐藏任务栏 | ✅ | 非持久化 SW_HIDE + 状态快照自愈 |
| FR-08 | 开机自启开关 | ✅ | 注册表 Run 键 + 设置开关 + 配置持久化 |
| FR-09 | 隐藏桌面图标 | ❌ | 可选项，未实现 |
| FR-10 | 防休眠/屏幕常亮 | ✅ | `SetThreadExecutionState` |
| FR-11 | 首页 TV 主界面 | ⚠️ | 网格菜单 + 顶部信息栏已做；壁纸/分类栏未做 |
| FR-12 | 右侧设置菜单 | ✅ | 设置抽屉（12 项） |
| FR-13 | 添加 APP | ⚠️ | 已安装列表 + 手动路径已做；原生文件对话框未做 |
| FR-14 | 应用管理 | ⚠️ | 排序（打开次数）已做；改名/换封面/删除/分类未做 |
| FR-15 | 启动外部程序 | ✅ | exe/lnk + Job Object 退出检测 + 前台激活 + 回焦点 |
| FR-16 | 音量控制 | ✅ | Core Audio + 音量键拦截 + OSD |
| FR-17 | 系统控制 | ✅ | 关机/重启/睡眠/锁屏/退出 + 确认框 |
| FR-18 | 配置持久化与备份 | ⚠️ | 配置持久化已做；备份/恢复未做 |

## 非功能需求（NFR）

| 编号 | 需求 | 状态 | 说明 |
|------|------|------|------|
| NFR-01 | 冷启动 ≤3s | ⚠️ | 未实测（需真机计时） |
| NFR-02 | 内存 ≤250MB / 24h 上涨 ≤80MB | ⚠️ | 未实测（需长期运行测试） |
| NFR-03 | 稳定性（崩溃自恢复） | ⚠️ | RegisterApplicationRestart + 状态快照自愈已做；WebView2 渲染进程监控未做 |
| NFR-04 | 远距离可读性 | ⚠️ | 字号已配（磁贴 20px+）；未完整验证基准 28px |
| NFR-05 | 健壮性（状态还原） | ✅ | 系统状态快照 + 下次启动自愈 + 一键恢复 |
| NFR-06 | 安全性（普通权限） | ✅ | 全程普通权限，无提权 |
| NFR-07 | 可维护性 | ✅ | 系统 API 全部收敛 Rust `win/` 模块 |
| NFR-08 | 分发（双形态） | ✅ | NSIS 安装版 + zip 便携版（portable.flag 切换本地 conf） |

## 真机验收项（投影环境待验证）

1. **4K 全屏覆盖完整**（无黑边/无边框）—— M1 已实测，投影环境需复核
2. **蓝牙遥控 / IR / 飞鼠 / 手柄** 四路输入真机测试—— 仅键盘验证过，真机设备待接入
3. **全屏独占游戏焦点**（Kodi 等）启动/退出焦点切换—— 已修复 .lnk 前台激活，真机复核
4. **长按返回+菜单 5 秒维护模式**—— 设置菜单入口已做，组合键入口待补
5. **24h 稳定性/内存**—— 未测
6. **WebView2 Runtime 依赖**—— Win10/11 内置，目标环境确认

## 遗留项（后续迭代）

- FR-05 按键重映射（keymap.json + 可编辑 UI）
- FR-09 隐藏桌面图标
- FR-11 壁纸 + 分类栏
- FR-13 原生文件浏览对话框
- FR-14 应用改名/换封面/删除/分类
- FR-18 配置备份/恢复
- NFR-03 WebView2 渲染进程监控（ProcessFailed）
- 维护模式长按组合键入口
